# Server Configuration
server.port=8080
server.port=${PORT:8080}


# # MySQL Database Configuration
# spring.datasource.url=jdbc:mysql://localhost:3306/goimay_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=123456

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=Z29JbWF5VmlldG5hbVNlY3JldEtleUZvckpXVEF1dGhlbnRpY2F0aW9uVG9rZW5HZW5lcmF0aW9uMjAyNFZlcnlMb25nQW5kU2VjdXJl
jwt.expiration=86400000

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,https://goimay.vercel.app

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Upload directory (lưu trong project để có thể commit vào git)
# Thay đổi từ user home sang thư mục trong project để đồng bộ giữa các máy
app.upload-dir=uploads

# Default Admin (tự tạo khi deploy, không cần đăng ký)
# Khuyến nghị: set trên môi trường deploy (Render/Railway/VM) qua biến ENV:
#   ADMIN_EMAIL, ADMIN_PASSWORD, ADMIN_NAME
app.admin.email=${ADMIN_EMAIL:admin@goimay.com}
app.admin.password=${ADMIN_PASSWORD:admin123}
app.admin.name=${ADMIN_NAME:Administrator}
app.admin.phone=${ADMIN_PHONE:0123456789}

# Cloudinary Configuration (lưu ảnh trên cloud)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dutnuguj4}
cloudinary.api-key=${CLOUDINARY_API_KEY:935732685759362}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:ch_IEA8V3M9uh1AAJ-_L1jkGpgw}

# Google OAuth Configuration
google.client-id=418375020384-9vvrvl88876fnl0dibbb1refqomc53gd.apps.googleusercontent.com

# Email Configuration (SMTP)
# For SendGrid: MAIL_HOST=smtp.sendgrid.net, MAIL_PORT=587, MAIL_USERNAME=apikey, MAIL_PASSWORD=<your-api-key>
# For Gmail: MAIL_HOST=smtp.gmail.com, MAIL_PORT=465, MAIL_USERNAME=<email>, MAIL_PASSWORD=<app-password>
spring.mail.host=${MAIL_HOST:smtp.sendgrid.net}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:apikey}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Email sender address (used in emails)
app.mail.from=${MAIL_FROM:noreply@goimay.com}
app.name=Gói Mây
